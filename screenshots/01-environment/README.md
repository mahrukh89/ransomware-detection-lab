# Environment Evidence

This folder contains the original environment captures. The source screenshots are unchanged.

| Capture | Timing visible in image | What it establishes |
|---|---|---|
| `01-windows-vm-system-information.png` | Capture time not visible | Windows 11 Pro lab endpoint and system information. |
| `02-hostname-network.jpeg` | Capture time not visible | Hostname/network command output; addresses are sanitized in the image. |
| `03-python-version.png` | Capture time not visible | Python 3.14.7 is available. |

No timestamp is assigned where the screenshot does not display one.
