# Final Validation Evidence

The original capture is retained unchanged.

| Capture | Timing visible in image | What it establishes |
|---|---|---|
| `20-final-validation.png` | Dashboard time range/plot is visible; a single capture timestamp is not displayed | Final Wazuh dashboard snapshot showing monitoring state, alert-level evolution, agents and MITRE-related dashboard information. |

It is not treated as a numeric ransomware-detection success metric because the capture does not isolate simulator-generated alerts or the repository's custom rule IDs.
